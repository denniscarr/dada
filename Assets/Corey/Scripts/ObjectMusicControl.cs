using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// OBSOLETE
/// </summary>
//
// OBSOLETE - PLACEHOLDER FOR PLAYER INTERACTION
//

public class ObjectMusicControl : MonoBehaviour {

	/*

	public GameObject[] sceneGameObjects;

	List<GameObject> soundObjects;


	void Start () {
		sceneGameObjects = FindObjectsOfType<GameObject> ();
	}
	

	void Update () {

		soundObjects = new List<GameObject>();

		foreach (GameObject thisObject in sceneGameObjects) {

			if (thisObject.name.Contains("SoundObject")) {
				soundObjects.Add(thisObject);
			}

			
		}

		if (Input.GetKeyDown (KeyCode.Alpha1)) {
			// rotate cube 1
			// switch start time to orig. start time + time/4

			soundObjects [0].transform.Rotate (new Vector3 (0, 90f, 0));

		} else if (Input.GetKeyDown (KeyCode.Alpha2)) {
			// rotate cube 2
			soundObjects [1].transform.Rotate (new Vector3 (0, 90f, 0));
		} else if (Input.GetKeyDown (KeyCode.Alpha3)) {
			// rotate cube 3
			soundObjects [2].transform.Rotate (new Vector3 (0, 90f, 0));
		}  

		if (Input.GetKeyDown (KeyCode.S)) {
			for (int i = 0; i < soundObjects.Count; i++) {
//				soundObjects [i].GetComponent<CS_MusicRotate> ().PlayObjectClip ();
			}
		}
		

	}

	public void UsedByPlayer() {

		for (int i = 0; i < soundObjects.Count; i++) {
			//soundObjects [i].GetComponent<CS_MusicRotate> ().PlayObjectClip ();
		}

	}
	*/



}
